from customized_inference import preProcessPC, inferenceBatch
from geometry import sampleFromMesh

def computeWholeFeature(pc, numParts, minPoints = 1024):
    partPCs = []
    
    min_radius = 0.1
    max_radius = 0.3
    for i in range(numParts):



def main():
    pass

if __name__ == '__main__':
    is_generate = True
    main(is_generate)